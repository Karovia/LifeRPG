import { PixelPanel } from '@/components/pixel'

/** 自动化简历/作品集模块 —— 占位页（resume 代理在此目录开发） */
export default function ResumePage() {
  return (
    <PixelPanel className="text-center">
      <h1 className="font-pixel text-sm text-wood-dark">RESUME</h1>
      <p className="mt-4 text-stone-dark">简历/作品集模块</p>
    </PixelPanel>
  )
}
