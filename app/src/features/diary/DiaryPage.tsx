import { PixelPanel } from '@/components/pixel'

/** 日记本模块 —— 占位页（diary 代理在此目录开发） */
export default function DiaryPage() {
  return (
    <PixelPanel className="text-center">
      <h1 className="font-pixel text-sm text-wood-dark">DIARY</h1>
      <p className="mt-4 text-stone-dark">日记本模块</p>
    </PixelPanel>
  )
}
