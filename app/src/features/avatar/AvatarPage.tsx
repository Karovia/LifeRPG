import { PixelPanel } from '@/components/pixel'

/** 虚拟形象模块 —— 占位页（avatar 代理在此目录开发） */
export default function AvatarPage() {
  return (
    <PixelPanel className="text-center">
      <h1 className="font-pixel text-sm text-wood-dark">AVATAR</h1>
      <p className="mt-4 text-stone-dark">虚拟形象模块</p>
    </PixelPanel>
  )
}
