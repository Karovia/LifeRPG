import { PixelPanel } from '@/components/pixel'

/** 小镇（占位）：NPC / 委托 / 家园玩法由小镇代理实现 */
export default function TownPage() {
  return (
    <PixelPanel>
      <h2 className="font-pixel text-sm text-ink">小镇</h2>
      <p className="mt-2 font-pixel text-xs text-stone">建设中……</p>
    </PixelPanel>
  )
}
